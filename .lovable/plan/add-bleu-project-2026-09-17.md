# Add Bleu project

## Changes
- Upload the three supplied Bleu images to the existing public project image library.
- Add a new project named **Bleu** with role **Brand Identity, Website, Merch**.
- Place Bleu first by shifting the current project order down without changing their relative order.
- Verify the new project data and image order.

## Technical details
- Reuse the existing `projects` content structure and `project-images` storage bucket.
- Use the wide logo image first, followed by the identity grid and website grid.
